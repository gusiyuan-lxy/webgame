<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>我的网站主页</title>
    <style>
        /* 主标题样式 */
        .main-title {
            color: #333;
            text-align: center;
            margin-top: 50px;
            font-size: 2.5em;
        }
        
        /* 按钮容器样式 */
        .button-container {
            display: flex;
            justify-content: center;
            gap: 20px;
            margin-top: 40px;
            flex-wrap: wrap; /* 允许按钮换行 */
        }
        
        /* 主按钮样式 - 此处可修改按钮颜色 */
        .primary-button {
            background-color: #4CAF50; /* 绿色 - 可修改为您想要的颜色 */
            color: white;
            padding: 12px 24px;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            font-size: 1em;
            transition: background-color 0.3s;
        }
        
        /* 次要按钮样式 */
        .secondary-button {
            background-color: #f44336; /* 红色 - 可修改为您想要的颜色 */
            color: white;
            padding: 12px 24px;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            font-size: 1em;
            transition: background-color 0.3s;
        }
        
        /* 游戏按钮样式 - 新增 */
        .game-button {
            background-color: #2196F3; /* 蓝色 - 可修改为您想要的颜色 */
            color: white;
            padding: 12px 24px;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            font-size: 1em;
            transition: background-color 0.3s;
        }
        
        /* 按钮悬停效果 */
        .primary-button:hover {
            background-color: #45a049; /* 悬停时颜色 - 与主按钮颜色匹配 */
        }
        
        .secondary-button:hover {
            background-color: #d32f2f; /* 悬停时颜色 - 与次要按钮颜色匹配 */
        }
        
        .game-button:hover {
            background-color: #0b7dda; /* 悬停时颜色 - 与游戏按钮颜色匹配 */
        }
        
        /* 译注提示样式 */
        .translation-note {
            font-style: italic;
            color: #666;
            text-align: center;
            margin-top: 20px;
            font-size: 0.9em;
            max-width: 800px;
            margin-left: auto;
            margin-right: auto;
            padding: 0 20px;
        }
        
        /* 新增：译注标题样式 */
        .note-title {
            font-weight: bold;
            color: #444;
            margin-bottom: 5px;
        }
    </style>
</head>
<body>
    <!-- 主标题 - 文案可修改 -->
    <h1 class="main-title">欢迎来到我的网站</h1>
    
    <!-- 按钮容器 -->
    <div class="button-container">
        <!-- 主要跳转按钮 - 文案可修改 -->
        <button class="primary-button" onclick="window.location.href='about.html'">关于我们</button>
        
        <!-- 次要跳转按钮 - 文案可修改 -->
        <button class="secondary-button" onclick="window.location.href='contact.html'">联系我们</button>
        
        <!-- 新增游戏列表按钮 -->
        <button class="game-button" onclick="window.location.href='games.html'">游戏列表</button>
    </div>
    
    <!-- 译注提示 - 文案可修改 -->
    <div class="translation-note">
        <div class="note-title">使用说明与注意事项：</div>
        <p>1. 本页面为网站主页，包含主要导航功能</p>
        <p>2. 按钮颜色可在CSS代码中修改，查找带有"可修改为您想要的颜色"注释的部分</p>
        <p>3. 游戏列表按钮为新增功能，点击可查看网站提供的游戏资源</p>
        <p>4. 所有跳转链接可根据实际需求修改</p>
        <p>5. 本页面已适配移动端显示，按钮在窄屏设备上会自动换行</p>
    </div>
    
    <script>
        // 可以在此处添加JavaScript功能
        console.log("页面加载完成");
        
        // 新增：游戏按钮点击事件（示例）
        document.querySelector('.game-button').addEventListener('click', function() {
            console.log("用户点击了游戏列表按钮");
            // 这里可以添加更多交互逻辑
        });
    </script>
</body>
</html>
